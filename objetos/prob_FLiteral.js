const d1 = {
  dia: 12,
  mes: 12,
  ano: 2022,
  dataCompleta: function () {
    return `${this.dia}/${this.mes}/${this.ano}`;
  },
};

const d2 = {
  dia: 2,
  mes: 2,
  ano: 2022,
  dataCompleta: function () {
    return `${this.dia}/${this.mes}/${this.ano}`;
  },
};

const d3 = {
  dia: 1,
  mes: 1,
  ano: 2022,
  dataCompleta: function () {
    return `${this.dia}/${this.mes}/${this.ano}`;
  },
};
